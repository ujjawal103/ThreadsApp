<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/new.css">
    <style>
        body{
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 20px;
        }
        h1{
            padding: 0 10px;
            background-color: rgb(66, 73, 170);
            color: greenyellow;
        }
        form{
            line-height: 30px;
        }
        div{
            background-color: rgb(47, 44, 44);
            margin-right: 30px;
            margin-bottom: 20px;
            padding: 10px;
            border-radius: 10px;
        }
        h2{
            color: aqua;
        }
        h4{
            color: rgb(231, 234, 23);
        }
    </style>
</head>
<body>
    <h1>!! Deploy Yor New Thoughts !!</h1>
    
   <form method="post" action="/posts/<%= post.id %>?_method=PATCH" class="newPost">        <!-- //action changed to PAtch -->
       <div>
        <h2>Author : @<%= post.username  %></h2>
        <h4>Post id : <%= post.id %></h4>
       </div>
        <textarea name="content" id="mainContent"><%= post.content %></textarea> <br> <br>
        <button class="Submit">Submit Post</button>
    </form>
</body>
</html>

<!-- note : - in route we declared patch request , but the problem is html form accept only GET or POST request . 
         to cure this 
                    we use a package of npm called method override. -->